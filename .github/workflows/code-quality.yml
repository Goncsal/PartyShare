name: Code Quality

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  code-quality:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        
    - name: Cache Maven dependencies
      uses: actions/cache@v4
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('tqsbackend/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2
        
    - name: Make Maven wrapper executable
      run: chmod +x tqsbackend/mvnw
      
    # Placeholder for SonarQube integration
    # When ready to add SonarQube, configure the following:
    #
    # - name: Run SonarQube analysis
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #   run: |
    #     cd tqsbackend
    #     ./mvnw clean verify sonar:sonar \
    #       -Dsonar.projectKey=your-project-key \
    #       -Dsonar.organization=your-org \
    #       -Dsonar.host.url=https://sonarcloud.io
    
    - name: Placeholder for code quality checks
      run: echo "Ready for SonarQube integration"
